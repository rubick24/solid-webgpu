{
  "name": "@rubick24/math",
  "license": "MIT",
  "description": "forked from gl-matrix v4 branch",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": {
      "default": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "rm -rf ./dist && rollup -c ./rollup.config.js && tsc",
    "watch": "concurrently \"rollup -w -c ./rollup.config.js\" \"tsc -w\""
  }
}
